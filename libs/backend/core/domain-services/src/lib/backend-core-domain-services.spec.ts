import { backendCoreDomainServices } from './backend-core-domain-services';

describe('backendCoreDomainServices', () => {
  it('should work', () => {
    expect(backendCoreDomainServices()).toEqual('backend-core-domain-services');
  });
});
