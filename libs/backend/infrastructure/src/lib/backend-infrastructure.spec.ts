import { backendInfrastructure } from './backend-infrastructure';

describe('backendInfrastructure', () => {
  it('should work', () => {
    expect(backendInfrastructure()).toEqual('backend-infrastructure');
  });
});
